# Copy to server/.env and replace every value with a distinct long random secret.
MYSQL_ROOT_PASSWORD=replace-with-a-strong-database-password
WEATHER_DEVICE_API_KEY=replace-with-a-long-random-device-key
FLASK_SECRET_KEY=replace-with-a-separate-long-random-flask-secret
